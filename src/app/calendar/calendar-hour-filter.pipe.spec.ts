import { CalendarHourFilterPipe } from './calendar-hour-filter.pipe';

describe('CalendarHourFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CalendarHourFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
